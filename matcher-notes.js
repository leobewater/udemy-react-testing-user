[{"content":"import { screen, render, within } from '@testing-library/react';\n\nfunction FormData() {\n  return (\n    <div>\n      <button>Go Back</button>\n      <form aria-label=\"form\">\n        <button>Save</button>\n        <button>Cancel</button>\n      </form>\n    </div>\n  );\n}\nrender(<FormData />);","type":"code","id":"wgufq"},{"content":"// Custom matcher\nfunction toContainRole(container, role, quantity = 1) {\n  const elements = within(container).queryAllByRole(role);\n  if (elements.length === quantity) {\n    return {\n      pass: true,\n    };\n  }\n\n  return {\n    pass: false,\n    message: () =>\n      `Expected to find ${quantity} ${role} elements. Found ${elements.length} instead.`,\n  };\n}\n\n// Extend custom matcher\nexpect.extend({ toContainRole });","type":"code","id":"sf3ah"},{"content":"test('the form displays two buttons', () => {\n  render(<FormData />);\n\n  const form = screen.getByRole('form');\n  // const buttons = within(form).getAllByRole('button');\n\n  // expect(buttons).toHaveLength(2);\n\n  // build own custom matcher\n  expect(form).toContainRole('button', 2);\n});","type":"code","id":"621a5"}]